#include <string.h>

/**
  * _strlen - returns length of a string
  *
  * @s: string to check length
  *
  * Return: int - length of string
  *
  */

int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
