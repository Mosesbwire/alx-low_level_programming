#include <stdio.h>

/**
  * main - entry point
  * @argc: count of arguments passed in
  * @argv: an array of string arguments passed in
  * Return: 0 - always (success)
  */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
